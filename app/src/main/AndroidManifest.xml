<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tiendav1">

<!--    <uses-feature android:name="android.hardware" android:required="false" />-->
<!--    <uses-feature android:name="android" android:required="false" />-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tiendav12"
        tools:targetApi="31">
        <activity
            android:name=".Admin_principal"
            android:exported="false"
            android:label="@string/title_activity_admin_principal"
            android:configChanges="uiMode"/>
        <activity
            android:name=".Principal_normal"
            android:exported="false"
            android:label="@string/title_activity_principal_normal"
            android:configChanges="uiMode"/>
        <activity
            android:name=".Registro"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>